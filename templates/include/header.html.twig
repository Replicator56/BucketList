<header>
    <nav>
        <a id="nav-logo-link" href="{{ path('main_home') }}">
            <img id="nav-logo" src="{{ asset('images/bucket.svg') }}" alt="Logo de Bucket-List">
        </a>

        <div id="nav-title">
            <a href="{{ path('main_home') }}">Bucket-List</a>
        </div>

        <div class="button-list">
            <p class="nav-button"><a href="{{ path('main_home') }}">Accueil</a></p>
            <p class="nav-button"><a href="{{ path('wish_all') }}">All wishes</a></p>
            <p class="nav-button"><a href="{{ path('wish_create') }}">Add yours !</a></p>
            <p class="nav-button"><a href="{{ path('main_about_us') }}">About Us</a></p>
        </div>
    </nav>
</header>
