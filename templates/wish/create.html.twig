{% extends 'base.html.twig' %}

{% block title %}
Add yours !
{% endblock %}

{% block main %}
    <main>
        <h1>Add your wishes !</h1>
        <li>
        <a href="{{ path('app_events') }}">Events</a></li>
        {{ form_start(wishForm) }}
        {{ form_widget(wishForm) }}
        <button type="submit">Create</button>
        {{ form_end(wishForm) }}
    </main>
{% endblock %}

