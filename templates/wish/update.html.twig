{% extends 'base.html.twig' %}

{% block title %}
    Update wish
{% endblock %}

{% block main %}
    <main>
        <h1>Update your wish!</h1>

        {{ form_start(wishForm) }}
        {{ form_widget(wishForm) }}

        <button type="submit">Update</button>
        {{ form_end(wishForm) }}
    </main>
{% endblock %}
